---
description: SVG générés sous prog/public — ASCII + entités XML, pas de collision de routes
globs: prog/public/**/*.svg
alwaysApply: true
---

# SVG publics (`prog/public/**/*.svg`)

Quand on crée ou modifie un schéma SVG pour le site :

1. **Fichier ASCII pur** (obligatoire)  
   - En-tête : `<?xml version="1.0" encoding="UTF-8"?>`  
   - Accents / tirets / ponctuation fine → **entités numériques XML** uniquement  
     (`é` → `&#233;`, `—` → `&#8212;`, `·` → `&#183;`, `’` → `&#8217;`, …)  
   - Interdit : octets UTF-8 multi-bytes dans le fichier (le parseur XML du navigateur peut lever `Encoding error` même si le fichier est UTF-8 valide)  
   - Écrire via Python et vérifier `content.encode("ascii")` avant de sauver

2. **XML bien formé** — après écriture : `npm run lint:prog`

3. **Chemins sans collision VitePress**  
   - Concepts génériques → `prog/public/concepts/…` (URL `/concepts/…`)  
   - Schémas de saison → `prog/public/diagrams/saison-YYYY/…` (URL `/diagrams/saison-YYYY/…`)  
   - **Jamais** `prog/public/saison-YYYY/` : collision avec la page Markdown `/saison-YYYY/`

4. Après génération : `npm run lint:prog`
